#!/usr/bin/nodejs
// prints out 3 statements

const strArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let idx = 0; idx < strArray.length; idx++) {
  console.log(strArray[idx]);
}
