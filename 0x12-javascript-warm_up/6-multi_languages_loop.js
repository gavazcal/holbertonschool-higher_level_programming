#!/usr/bin/nodejs
// prints 3 lines with one print and a loop

const strings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let idx = 0; idx < strings.length; idx++) {
  console.log(strings[idx]);
}
